import React from "react";
import ReactDOM from "react-dom/client";
import FilmeMais from "./App";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <FilmeMais />
  </React.StrictMode>
);
